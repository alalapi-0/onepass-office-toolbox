# fs_demo

用于测试 `fs_tools` 中的文件重命名和按扩展名整理功能。

示例命令：

```bash
python -m onepass_office_toolbox.cli fs rename-basic --dir sample_data/fs_demo --dry-run
python -m onepass_office_toolbox.cli fs organize-ext --dir sample_data/fs_demo --dry-run
```
